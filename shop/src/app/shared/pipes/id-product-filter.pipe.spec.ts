import { IdProductFilterPipe } from './id-product-filter.pipe';

describe('IdProductFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IdProductFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
