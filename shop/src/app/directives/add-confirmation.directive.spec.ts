import { AddConfirmationDirective } from './add-confirmation.directive';

describe('AddConfirmationDirective', () => {
  it('should create an instance', () => {
    const directive = new AddConfirmationDirective();
    expect(directive).toBeTruthy();
  });
});
